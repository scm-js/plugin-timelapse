// Generated by npm run build - do not edit; the source is plugin.ts.
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __commonJS = (cb, mod) => function __require() {
  try {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  } catch (e) {
    throw mod = 0, e;
  }
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// node_modules/gifenc/dist/gifenc.js
var require_gifenc = __commonJS({
  "node_modules/gifenc/dist/gifenc.js"(exports) {
    var __defProp2 = Object.defineProperty;
    var __markAsModule = (target) => __defProp2(target, "__esModule", { value: true });
    var __export = (target, all) => {
      for (var name in all)
        __defProp2(target, name, { get: all[name], enumerable: true });
    };
    __markAsModule(exports);
    __export(exports, {
      GIFEncoder: () => GIFEncoder2,
      applyPalette: () => applyPalette2,
      default: () => src_default,
      nearestColor: () => nearestColor,
      nearestColorIndex: () => nearestColorIndex,
      nearestColorIndexWithDistance: () => nearestColorIndexWithDistance,
      prequantize: () => prequantize,
      quantize: () => quantize2,
      snapColorsToPalette: () => snapColorsToPalette
    });
    var constants_default = {
      signature: "GIF",
      version: "89a",
      trailer: 59,
      extensionIntroducer: 33,
      applicationExtensionLabel: 255,
      graphicControlExtensionLabel: 249,
      imageSeparator: 44,
      signatureSize: 3,
      versionSize: 3,
      globalColorTableFlagMask: 128,
      colorResolutionMask: 112,
      sortFlagMask: 8,
      globalColorTableSizeMask: 7,
      applicationIdentifierSize: 8,
      applicationAuthCodeSize: 3,
      disposalMethodMask: 28,
      userInputFlagMask: 2,
      transparentColorFlagMask: 1,
      localColorTableFlagMask: 128,
      interlaceFlagMask: 64,
      idSortFlagMask: 32,
      localColorTableSizeMask: 7
    };
    function createStream(initialCapacity = 256) {
      let cursor = 0;
      let contents = new Uint8Array(initialCapacity);
      return {
        get buffer() {
          return contents.buffer;
        },
        reset() {
          cursor = 0;
        },
        bytesView() {
          return contents.subarray(0, cursor);
        },
        bytes() {
          return contents.slice(0, cursor);
        },
        writeByte(byte) {
          expand(cursor + 1);
          contents[cursor] = byte;
          cursor++;
        },
        writeBytes(data, offset = 0, byteLength = data.length) {
          expand(cursor + byteLength);
          for (let i = 0; i < byteLength; i++) {
            contents[cursor++] = data[i + offset];
          }
        },
        writeBytesView(data, offset = 0, byteLength = data.byteLength) {
          expand(cursor + byteLength);
          contents.set(data.subarray(offset, offset + byteLength), cursor);
          cursor += byteLength;
        }
      };
      function expand(newCapacity) {
        var prevCapacity = contents.length;
        if (prevCapacity >= newCapacity)
          return;
        var CAPACITY_DOUBLING_MAX = 1024 * 1024;
        newCapacity = Math.max(newCapacity, prevCapacity * (prevCapacity < CAPACITY_DOUBLING_MAX ? 2 : 1.125) >>> 0);
        if (prevCapacity != 0)
          newCapacity = Math.max(newCapacity, 256);
        const oldContents = contents;
        contents = new Uint8Array(newCapacity);
        if (cursor > 0)
          contents.set(oldContents.subarray(0, cursor), 0);
      }
    }
    var BITS = 12;
    var DEFAULT_HSIZE = 5003;
    var MASKS = [
      0,
      1,
      3,
      7,
      15,
      31,
      63,
      127,
      255,
      511,
      1023,
      2047,
      4095,
      8191,
      16383,
      32767,
      65535
    ];
    function lzwEncode(width, height, pixels, colorDepth, outStream = createStream(512), accum = new Uint8Array(256), htab = new Int32Array(DEFAULT_HSIZE), codetab = new Int32Array(DEFAULT_HSIZE)) {
      const hsize = htab.length;
      const initCodeSize = Math.max(2, colorDepth);
      accum.fill(0);
      codetab.fill(0);
      htab.fill(-1);
      let cur_accum = 0;
      let cur_bits = 0;
      const init_bits = initCodeSize + 1;
      const g_init_bits = init_bits;
      let clear_flg = false;
      let n_bits = g_init_bits;
      let maxcode = (1 << n_bits) - 1;
      const ClearCode = 1 << init_bits - 1;
      const EOFCode = ClearCode + 1;
      let free_ent = ClearCode + 2;
      let a_count = 0;
      let ent = pixels[0];
      let hshift = 0;
      for (let fcode = hsize; fcode < 65536; fcode *= 2) {
        ++hshift;
      }
      hshift = 8 - hshift;
      outStream.writeByte(initCodeSize);
      output(ClearCode);
      const length = pixels.length;
      for (let idx = 1; idx < length; idx++) {
        next_block: {
          const c = pixels[idx];
          const fcode = (c << BITS) + ent;
          let i = c << hshift ^ ent;
          if (htab[i] === fcode) {
            ent = codetab[i];
            break next_block;
          }
          const disp = i === 0 ? 1 : hsize - i;
          while (htab[i] >= 0) {
            i -= disp;
            if (i < 0)
              i += hsize;
            if (htab[i] === fcode) {
              ent = codetab[i];
              break next_block;
            }
          }
          output(ent);
          ent = c;
          if (free_ent < 1 << BITS) {
            codetab[i] = free_ent++;
            htab[i] = fcode;
          } else {
            htab.fill(-1);
            free_ent = ClearCode + 2;
            clear_flg = true;
            output(ClearCode);
          }
        }
      }
      output(ent);
      output(EOFCode);
      outStream.writeByte(0);
      return outStream.bytesView();
      function output(code) {
        cur_accum &= MASKS[cur_bits];
        if (cur_bits > 0)
          cur_accum |= code << cur_bits;
        else
          cur_accum = code;
        cur_bits += n_bits;
        while (cur_bits >= 8) {
          accum[a_count++] = cur_accum & 255;
          if (a_count >= 254) {
            outStream.writeByte(a_count);
            outStream.writeBytesView(accum, 0, a_count);
            a_count = 0;
          }
          cur_accum >>= 8;
          cur_bits -= 8;
        }
        if (free_ent > maxcode || clear_flg) {
          if (clear_flg) {
            n_bits = g_init_bits;
            maxcode = (1 << n_bits) - 1;
            clear_flg = false;
          } else {
            ++n_bits;
            maxcode = n_bits === BITS ? 1 << n_bits : (1 << n_bits) - 1;
          }
        }
        if (code == EOFCode) {
          while (cur_bits > 0) {
            accum[a_count++] = cur_accum & 255;
            if (a_count >= 254) {
              outStream.writeByte(a_count);
              outStream.writeBytesView(accum, 0, a_count);
              a_count = 0;
            }
            cur_accum >>= 8;
            cur_bits -= 8;
          }
          if (a_count > 0) {
            outStream.writeByte(a_count);
            outStream.writeBytesView(accum, 0, a_count);
            a_count = 0;
          }
        }
      }
    }
    var lzwEncode_default = lzwEncode;
    function rgb888_to_rgb565(r, g, b) {
      return r << 8 & 63488 | g << 2 & 992 | b >> 3;
    }
    function rgba8888_to_rgba4444(r, g, b, a) {
      return r >> 4 | g & 240 | (b & 240) << 4 | (a & 240) << 8;
    }
    function rgb888_to_rgb444(r, g, b) {
      return r >> 4 << 8 | g & 240 | b >> 4;
    }
    function clamp(value, min, max) {
      return value < min ? min : value > max ? max : value;
    }
    function sqr(value) {
      return value * value;
    }
    function find_nn(bins, idx, hasAlpha) {
      var nn = 0;
      var err = 1e100;
      const bin1 = bins[idx];
      const n1 = bin1.cnt;
      const wa = bin1.ac;
      const wr = bin1.rc;
      const wg = bin1.gc;
      const wb = bin1.bc;
      for (var i = bin1.fw; i != 0; i = bins[i].fw) {
        const bin = bins[i];
        const n2 = bin.cnt;
        const nerr2 = n1 * n2 / (n1 + n2);
        if (nerr2 >= err)
          continue;
        var nerr = 0;
        if (hasAlpha) {
          nerr += nerr2 * sqr(bin.ac - wa);
          if (nerr >= err)
            continue;
        }
        nerr += nerr2 * sqr(bin.rc - wr);
        if (nerr >= err)
          continue;
        nerr += nerr2 * sqr(bin.gc - wg);
        if (nerr >= err)
          continue;
        nerr += nerr2 * sqr(bin.bc - wb);
        if (nerr >= err)
          continue;
        err = nerr;
        nn = i;
      }
      bin1.err = err;
      bin1.nn = nn;
    }
    function create_bin() {
      return {
        ac: 0,
        rc: 0,
        gc: 0,
        bc: 0,
        cnt: 0,
        nn: 0,
        fw: 0,
        bk: 0,
        tm: 0,
        mtm: 0,
        err: 0
      };
    }
    function create_bin_list(data, format) {
      const bincount = format === "rgb444" ? 4096 : 65536;
      const bins = new Array(bincount);
      const size = data.length;
      if (format === "rgba4444") {
        for (let i = 0; i < size; ++i) {
          const color = data[i];
          const a = color >> 24 & 255;
          const b = color >> 16 & 255;
          const g = color >> 8 & 255;
          const r = color & 255;
          const index = rgba8888_to_rgba4444(r, g, b, a);
          let bin = index in bins ? bins[index] : bins[index] = create_bin();
          bin.rc += r;
          bin.gc += g;
          bin.bc += b;
          bin.ac += a;
          bin.cnt++;
        }
      } else if (format === "rgb444") {
        for (let i = 0; i < size; ++i) {
          const color = data[i];
          const b = color >> 16 & 255;
          const g = color >> 8 & 255;
          const r = color & 255;
          const index = rgb888_to_rgb444(r, g, b);
          let bin = index in bins ? bins[index] : bins[index] = create_bin();
          bin.rc += r;
          bin.gc += g;
          bin.bc += b;
          bin.cnt++;
        }
      } else {
        for (let i = 0; i < size; ++i) {
          const color = data[i];
          const b = color >> 16 & 255;
          const g = color >> 8 & 255;
          const r = color & 255;
          const index = rgb888_to_rgb565(r, g, b);
          let bin = index in bins ? bins[index] : bins[index] = create_bin();
          bin.rc += r;
          bin.gc += g;
          bin.bc += b;
          bin.cnt++;
        }
      }
      return bins;
    }
    function quantize2(rgba, maxColors, opts = {}) {
      const {
        format = "rgb565",
        clearAlpha = true,
        clearAlphaColor = 0,
        clearAlphaThreshold = 0,
        oneBitAlpha = false
      } = opts;
      if (!rgba || !rgba.buffer) {
        throw new Error("quantize() expected RGBA Uint8Array data");
      }
      if (!(rgba instanceof Uint8Array) && !(rgba instanceof Uint8ClampedArray)) {
        throw new Error("quantize() expected RGBA Uint8Array data");
      }
      const data = new Uint32Array(rgba.buffer);
      let useSqrt = opts.useSqrt !== false;
      const hasAlpha = format === "rgba4444";
      const bins = create_bin_list(data, format);
      const bincount = bins.length;
      const bincountMinusOne = bincount - 1;
      const heap = new Uint32Array(bincount + 1);
      var maxbins = 0;
      for (var i = 0; i < bincount; ++i) {
        const bin = bins[i];
        if (bin != null) {
          var d = 1 / bin.cnt;
          if (hasAlpha)
            bin.ac *= d;
          bin.rc *= d;
          bin.gc *= d;
          bin.bc *= d;
          bins[maxbins++] = bin;
        }
      }
      if (sqr(maxColors) / maxbins < 0.022) {
        useSqrt = false;
      }
      var i = 0;
      for (; i < maxbins - 1; ++i) {
        bins[i].fw = i + 1;
        bins[i + 1].bk = i;
        if (useSqrt)
          bins[i].cnt = Math.sqrt(bins[i].cnt);
      }
      if (useSqrt)
        bins[i].cnt = Math.sqrt(bins[i].cnt);
      var h2, l, l2;
      for (i = 0; i < maxbins; ++i) {
        find_nn(bins, i, false);
        var err = bins[i].err;
        for (l = ++heap[0]; l > 1; l = l2) {
          l2 = l >> 1;
          if (bins[h2 = heap[l2]].err <= err)
            break;
          heap[l] = h2;
        }
        heap[l] = i;
      }
      var extbins = maxbins - maxColors;
      for (i = 0; i < extbins; ) {
        var tb;
        for (; ; ) {
          var b1 = heap[1];
          tb = bins[b1];
          if (tb.tm >= tb.mtm && bins[tb.nn].mtm <= tb.tm)
            break;
          if (tb.mtm == bincountMinusOne)
            b1 = heap[1] = heap[heap[0]--];
          else {
            find_nn(bins, b1, false);
            tb.tm = i;
          }
          var err = bins[b1].err;
          for (l = 1; (l2 = l + l) <= heap[0]; l = l2) {
            if (l2 < heap[0] && bins[heap[l2]].err > bins[heap[l2 + 1]].err)
              l2++;
            if (err <= bins[h2 = heap[l2]].err)
              break;
            heap[l] = h2;
          }
          heap[l] = b1;
        }
        var nb = bins[tb.nn];
        var n1 = tb.cnt;
        var n2 = nb.cnt;
        var d = 1 / (n1 + n2);
        if (hasAlpha)
          tb.ac = d * (n1 * tb.ac + n2 * nb.ac);
        tb.rc = d * (n1 * tb.rc + n2 * nb.rc);
        tb.gc = d * (n1 * tb.gc + n2 * nb.gc);
        tb.bc = d * (n1 * tb.bc + n2 * nb.bc);
        tb.cnt += nb.cnt;
        tb.mtm = ++i;
        bins[nb.bk].fw = nb.fw;
        bins[nb.fw].bk = nb.bk;
        nb.mtm = bincountMinusOne;
      }
      let palette = [];
      var k = 0;
      for (i = 0; ; ++k) {
        let r = clamp(Math.round(bins[i].rc), 0, 255);
        let g = clamp(Math.round(bins[i].gc), 0, 255);
        let b = clamp(Math.round(bins[i].bc), 0, 255);
        let a = 255;
        if (hasAlpha) {
          a = clamp(Math.round(bins[i].ac), 0, 255);
          if (oneBitAlpha) {
            const threshold = typeof oneBitAlpha === "number" ? oneBitAlpha : 127;
            a = a <= threshold ? 0 : 255;
          }
          if (clearAlpha && a <= clearAlphaThreshold) {
            r = g = b = clearAlphaColor;
            a = 0;
          }
        }
        const color = hasAlpha ? [r, g, b, a] : [r, g, b];
        const exists = existsInPalette(palette, color);
        if (!exists)
          palette.push(color);
        if ((i = bins[i].fw) == 0)
          break;
      }
      return palette;
    }
    function existsInPalette(palette, color) {
      for (let i = 0; i < palette.length; i++) {
        const p = palette[i];
        let matchesRGB = p[0] === color[0] && p[1] === color[1] && p[2] === color[2];
        let matchesAlpha = p.length >= 4 && color.length >= 4 ? p[3] === color[3] : true;
        if (matchesRGB && matchesAlpha)
          return true;
      }
      return false;
    }
    function euclideanDistanceSquared(a, b) {
      var sum = 0;
      var n;
      for (n = 0; n < a.length; n++) {
        const dx = a[n] - b[n];
        sum += dx * dx;
      }
      return sum;
    }
    function roundStep(byte, step) {
      return step > 1 ? Math.round(byte / step) * step : byte;
    }
    function prequantize(rgba, { roundRGB = 5, roundAlpha = 10, oneBitAlpha = null } = {}) {
      const data = new Uint32Array(rgba.buffer);
      for (let i = 0; i < data.length; i++) {
        const color = data[i];
        let a = color >> 24 & 255;
        let b = color >> 16 & 255;
        let g = color >> 8 & 255;
        let r = color & 255;
        a = roundStep(a, roundAlpha);
        if (oneBitAlpha) {
          const threshold = typeof oneBitAlpha === "number" ? oneBitAlpha : 127;
          a = a <= threshold ? 0 : 255;
        }
        r = roundStep(r, roundRGB);
        g = roundStep(g, roundRGB);
        b = roundStep(b, roundRGB);
        data[i] = a << 24 | b << 16 | g << 8 | r << 0;
      }
    }
    function applyPalette2(rgba, palette, format = "rgb565") {
      if (!rgba || !rgba.buffer) {
        throw new Error("quantize() expected RGBA Uint8Array data");
      }
      if (!(rgba instanceof Uint8Array) && !(rgba instanceof Uint8ClampedArray)) {
        throw new Error("quantize() expected RGBA Uint8Array data");
      }
      if (palette.length > 256) {
        throw new Error("applyPalette() only works with 256 colors or less");
      }
      const data = new Uint32Array(rgba.buffer);
      const length = data.length;
      const bincount = format === "rgb444" ? 4096 : 65536;
      const index = new Uint8Array(length);
      const cache = new Array(bincount);
      const hasAlpha = format === "rgba4444";
      if (format === "rgba4444") {
        for (let i = 0; i < length; i++) {
          const color = data[i];
          const a = color >> 24 & 255;
          const b = color >> 16 & 255;
          const g = color >> 8 & 255;
          const r = color & 255;
          const key = rgba8888_to_rgba4444(r, g, b, a);
          const idx = key in cache ? cache[key] : cache[key] = nearestColorIndexRGBA(r, g, b, a, palette);
          index[i] = idx;
        }
      } else {
        const rgb888_to_key = format === "rgb444" ? rgb888_to_rgb444 : rgb888_to_rgb565;
        for (let i = 0; i < length; i++) {
          const color = data[i];
          const b = color >> 16 & 255;
          const g = color >> 8 & 255;
          const r = color & 255;
          const key = rgb888_to_key(r, g, b);
          const idx = key in cache ? cache[key] : cache[key] = nearestColorIndexRGB(r, g, b, palette);
          index[i] = idx;
        }
      }
      return index;
    }
    function nearestColorIndexRGBA(r, g, b, a, palette) {
      let k = 0;
      let mindist = 1e100;
      for (let i = 0; i < palette.length; i++) {
        const px2 = palette[i];
        const a2 = px2[3];
        let curdist = sqr2(a2 - a);
        if (curdist > mindist)
          continue;
        const r2 = px2[0];
        curdist += sqr2(r2 - r);
        if (curdist > mindist)
          continue;
        const g2 = px2[1];
        curdist += sqr2(g2 - g);
        if (curdist > mindist)
          continue;
        const b2 = px2[2];
        curdist += sqr2(b2 - b);
        if (curdist > mindist)
          continue;
        mindist = curdist;
        k = i;
      }
      return k;
    }
    function nearestColorIndexRGB(r, g, b, palette) {
      let k = 0;
      let mindist = 1e100;
      for (let i = 0; i < palette.length; i++) {
        const px2 = palette[i];
        const r2 = px2[0];
        let curdist = sqr2(r2 - r);
        if (curdist > mindist)
          continue;
        const g2 = px2[1];
        curdist += sqr2(g2 - g);
        if (curdist > mindist)
          continue;
        const b2 = px2[2];
        curdist += sqr2(b2 - b);
        if (curdist > mindist)
          continue;
        mindist = curdist;
        k = i;
      }
      return k;
    }
    function snapColorsToPalette(palette, knownColors, threshold = 5) {
      if (!palette.length || !knownColors.length)
        return;
      const paletteRGB = palette.map((p) => p.slice(0, 3));
      const thresholdSq = threshold * threshold;
      const dim = palette[0].length;
      for (let i = 0; i < knownColors.length; i++) {
        let color = knownColors[i];
        if (color.length < dim) {
          color = [color[0], color[1], color[2], 255];
        } else if (color.length > dim) {
          color = color.slice(0, 3);
        } else {
          color = color.slice();
        }
        const r = nearestColorIndexWithDistance(paletteRGB, color.slice(0, 3), euclideanDistanceSquared);
        const idx = r[0];
        const distanceSq = r[1];
        if (distanceSq > 0 && distanceSq <= thresholdSq) {
          palette[idx] = color;
        }
      }
    }
    function sqr2(a) {
      return a * a;
    }
    function nearestColorIndex(colors, pixel, distanceFn = euclideanDistanceSquared) {
      let minDist = Infinity;
      let minDistIndex = -1;
      for (let j = 0; j < colors.length; j++) {
        const paletteColor = colors[j];
        const dist = distanceFn(pixel, paletteColor);
        if (dist < minDist) {
          minDist = dist;
          minDistIndex = j;
        }
      }
      return minDistIndex;
    }
    function nearestColorIndexWithDistance(colors, pixel, distanceFn = euclideanDistanceSquared) {
      let minDist = Infinity;
      let minDistIndex = -1;
      for (let j = 0; j < colors.length; j++) {
        const paletteColor = colors[j];
        const dist = distanceFn(pixel, paletteColor);
        if (dist < minDist) {
          minDist = dist;
          minDistIndex = j;
        }
      }
      return [minDistIndex, minDist];
    }
    function nearestColor(colors, pixel, distanceFn = euclideanDistanceSquared) {
      return colors[nearestColorIndex(colors, pixel, distanceFn)];
    }
    function GIFEncoder2(opt = {}) {
      const { initialCapacity = 4096, auto = true } = opt;
      const stream = createStream(initialCapacity);
      const HSIZE = 5003;
      const accum = new Uint8Array(256);
      const htab = new Int32Array(HSIZE);
      const codetab = new Int32Array(HSIZE);
      let hasInit = false;
      return {
        reset() {
          stream.reset();
          hasInit = false;
        },
        finish() {
          stream.writeByte(constants_default.trailer);
        },
        bytes() {
          return stream.bytes();
        },
        bytesView() {
          return stream.bytesView();
        },
        get buffer() {
          return stream.buffer;
        },
        get stream() {
          return stream;
        },
        writeHeader,
        writeFrame(index, width, height, opts = {}) {
          const {
            transparent = false,
            transparentIndex = 0,
            delay = 0,
            palette = null,
            repeat = 0,
            colorDepth = 8,
            dispose = -1
          } = opts;
          let first = false;
          if (auto) {
            if (!hasInit) {
              first = true;
              writeHeader();
              hasInit = true;
            }
          } else {
            first = Boolean(opts.first);
          }
          width = Math.max(0, Math.floor(width));
          height = Math.max(0, Math.floor(height));
          if (first) {
            if (!palette) {
              throw new Error("First frame must include a { palette } option");
            }
            encodeLogicalScreenDescriptor(stream, width, height, palette, colorDepth);
            encodeColorTable(stream, palette);
            if (repeat >= 0) {
              encodeNetscapeExt(stream, repeat);
            }
          }
          const delayTime = Math.round(delay / 10);
          encodeGraphicControlExt(stream, dispose, delayTime, transparent, transparentIndex);
          const useLocalColorTable = Boolean(palette) && !first;
          encodeImageDescriptor(stream, width, height, useLocalColorTable ? palette : null);
          if (useLocalColorTable)
            encodeColorTable(stream, palette);
          encodePixels(stream, index, width, height, colorDepth, accum, htab, codetab);
        }
      };
      function writeHeader() {
        writeUTFBytes(stream, "GIF89a");
      }
    }
    function encodeGraphicControlExt(stream, dispose, delay, transparent, transparentIndex) {
      stream.writeByte(33);
      stream.writeByte(249);
      stream.writeByte(4);
      if (transparentIndex < 0) {
        transparentIndex = 0;
        transparent = false;
      }
      var transp, disp;
      if (!transparent) {
        transp = 0;
        disp = 0;
      } else {
        transp = 1;
        disp = 2;
      }
      if (dispose >= 0) {
        disp = dispose & 7;
      }
      disp <<= 2;
      const userInput = 0;
      stream.writeByte(0 | disp | userInput | transp);
      writeUInt16(stream, delay);
      stream.writeByte(transparentIndex || 0);
      stream.writeByte(0);
    }
    function encodeLogicalScreenDescriptor(stream, width, height, palette, colorDepth = 8) {
      const globalColorTableFlag = 1;
      const sortFlag = 0;
      const globalColorTableSize = colorTableSize(palette.length) - 1;
      const fields = globalColorTableFlag << 7 | colorDepth - 1 << 4 | sortFlag << 3 | globalColorTableSize;
      const backgroundColorIndex = 0;
      const pixelAspectRatio = 0;
      writeUInt16(stream, width);
      writeUInt16(stream, height);
      stream.writeBytes([fields, backgroundColorIndex, pixelAspectRatio]);
    }
    function encodeNetscapeExt(stream, repeat) {
      stream.writeByte(33);
      stream.writeByte(255);
      stream.writeByte(11);
      writeUTFBytes(stream, "NETSCAPE2.0");
      stream.writeByte(3);
      stream.writeByte(1);
      writeUInt16(stream, repeat);
      stream.writeByte(0);
    }
    function encodeColorTable(stream, palette) {
      const colorTableLength = 1 << colorTableSize(palette.length);
      for (let i = 0; i < colorTableLength; i++) {
        let color = [0, 0, 0];
        if (i < palette.length) {
          color = palette[i];
        }
        stream.writeByte(color[0]);
        stream.writeByte(color[1]);
        stream.writeByte(color[2]);
      }
    }
    function encodeImageDescriptor(stream, width, height, localPalette) {
      stream.writeByte(44);
      writeUInt16(stream, 0);
      writeUInt16(stream, 0);
      writeUInt16(stream, width);
      writeUInt16(stream, height);
      if (localPalette) {
        const interlace = 0;
        const sorted = 0;
        const palSize = colorTableSize(localPalette.length) - 1;
        stream.writeByte(128 | interlace | sorted | 0 | palSize);
      } else {
        stream.writeByte(0);
      }
    }
    function encodePixels(stream, index, width, height, colorDepth = 8, accum, htab, codetab) {
      lzwEncode_default(width, height, index, colorDepth, stream, accum, htab, codetab);
    }
    function writeUInt16(stream, short) {
      stream.writeByte(short & 255);
      stream.writeByte(short >> 8 & 255);
    }
    function writeUTFBytes(stream, text) {
      for (var i = 0; i < text.length; i++) {
        stream.writeByte(text.charCodeAt(i));
      }
    }
    function colorTableSize(length) {
      return Math.max(Math.ceil(Math.log2(length)), 1);
    }
    var src_default = GIFEncoder2;
  }
});

// export.ts
var import_gifenc = __toESM(require_gifenc(), 1);

// recording.ts
var KEY_EVERY = 64;
var UNIT_STRIDE = 4;
var DOODAD_STRIDE = 4;
var SPRITE_STRIDE = 5;
var LOCATION_STRIDE = 4;
var IDLE_CAP = 6e4;
function packObjects(state) {
  const units = new Int32Array(state.units.length * UNIT_STRIDE);
  state.units.forEach((u, i) => units.set([u.unitId, u.owner, u.x, u.y], i * UNIT_STRIDE));
  const doodads = new Int32Array(state.doodads.length * DOODAD_STRIDE);
  state.doodads.forEach((d, i) => doodads.set([d.doodadId, d.owner, d.x, d.y], i * DOODAD_STRIDE));
  const sprites = new Int32Array(state.sprites.length * SPRITE_STRIDE);
  state.sprites.forEach((s, i) => sprites.set([s.spriteId, s.flags, s.owner, s.x, s.y], i * SPRITE_STRIDE));
  const used = state.locations.filter((l, i) => i !== 63 && (l.left | l.top | l.right | l.bottom) !== 0);
  const locations = new Int32Array(used.length * LOCATION_STRIDE);
  used.forEach((l, i) => locations.set([l.left, l.top, l.right, l.bottom], i * LOCATION_STRIDE));
  return { units, doodads, sprites, locations };
}
function sameArray(a, b) {
  if (a.length !== b.length) return false;
  for (let i = 0; i < a.length; i++) if (a[i] !== b[i]) return false;
  return true;
}
function sameObjects(a, b) {
  return sameArray(a.units, b.units) && sameArray(a.doodads, b.doodads) && sameArray(a.sprites, b.sprites) && sameArray(a.locations, b.locations);
}
function diffRect(a, b, width) {
  let x0 = Infinity, y0 = Infinity, x1 = -1, y1 = -1;
  const n = Math.min(a.length, b.length);
  for (let i = 0; i < n; i++) {
    if (a[i] === b[i]) continue;
    const x = i % width, y = (i - x) / width;
    if (x < x0) x0 = x;
    if (x > x1) x1 = x;
    if (y < y0) y0 = y;
    y1 = y;
  }
  return x1 < 0 ? null : { x0, y0, x1: x1 + 1, y1: y1 + 1 };
}
function cutRect(tiles, width, rect) {
  const w = rect.x1 - rect.x0;
  const out = new Uint16Array(w * (rect.y1 - rect.y0));
  for (let y = rect.y0; y < rect.y1; y++) out.set(tiles.subarray(y * width + rect.x0, y * width + rect.x1), (y - rect.y0) * w);
  return out;
}
function pasteRect(tiles, width, rect, cells) {
  const w = rect.x1 - rect.x0;
  for (let y = rect.y0; y < rect.y1; y++) tiles.set(cells.subarray((y - rect.y0) * w, (y - rect.y0 + 1) * w), y * width + rect.x0);
}
function sameSnapshot(a, b) {
  return a.width === b.width && a.height === b.height && a.era === b.era && a.tiles.length === b.tiles.length && diffRect(a.tiles, b.tiles, a.width) === null && sameObjects(a.objects, b.objects);
}
var Recorder = class _Recorder {
  tiles;
  objects;
  width;
  height;
  era;
  /** Frames taken so far, the start frame included — the next frame's index. */
  count;
  /** Editing time so far, and the clock reading it was last advanced at. */
  t;
  lastClock;
  constructor(start, count, t, clock) {
    this.tiles = start.tiles.slice();
    this.objects = start.objects;
    this.width = start.width;
    this.height = start.height;
    this.era = start.era;
    this.count = count;
    this.t = t;
    this.lastClock = clock;
  }
  /** A fresh recording's first frame: the map before anything was recorded. */
  static start(state, clock) {
    const objects = packObjects(state);
    const recorder = new _Recorder({ tiles: state.tiles, objects, width: state.width, height: state.height, era: state.era }, 1, 0, clock);
    const frame = {
      t: 0,
      reason: "start",
      label: "",
      area: null,
      width: state.width,
      height: state.height,
      era: state.era,
      key: state.tiles.slice(),
      objects
    };
    return { recorder, frame };
  }
  /** Where the recorder has got to: the map as its last frame shows it. Not a copy — read it, do not keep it. */
  state() {
    return { tiles: this.tiles, objects: this.objects, width: this.width, height: this.height, era: this.era };
  }
  /** Editing time so far. */
  elapsed() {
    return this.t;
  }
  /**
   * The frame for the map after a commit, or null when nothing visible changed (a dialog's
   * tables, a location renamed, an edit undone by the next one before it was seen).
   */
  take(state, event, clock) {
    const resized = state.width !== this.width || state.height !== this.height || state.era !== this.era;
    const objects = packObjects(state);
    const objectsChanged = !sameObjects(objects, this.objects);
    const rect = resized ? null : diffRect(this.tiles, state.tiles, this.width);
    if (!resized && !rect && !objectsChanged) return null;
    this.t += Math.min(IDLE_CAP, Math.max(0, clock - this.lastClock));
    this.lastClock = clock;
    const frame = {
      t: this.t,
      reason: event.reason,
      label: event.label,
      area: event.area ? { ...event.area } : null,
      width: state.width,
      height: state.height,
      era: state.era
    };
    if (resized || this.count % KEY_EVERY === 0) frame.key = state.tiles.slice();
    else if (rect) frame.patch = { rect, tiles: cutRect(state.tiles, state.width, rect) };
    if (objectsChanged || resized) frame.objects = objects;
    this.tiles = state.tiles.slice();
    this.objects = objects;
    this.width = state.width;
    this.height = state.height;
    this.era = state.era;
    this.count++;
    return frame;
  }
  /** Let the clock catch up without taking a frame — a map coming back to the front after a while behind. */
  touch(clock) {
    this.lastClock = clock;
  }
};
var EMPTY_OBJECTS = { units: new Int32Array(0), doodads: new Int32Array(0), sprites: new Int32Array(0), locations: new Int32Array(0) };
function union(a, b) {
  if (a === "all") return a;
  if (!a) return { ...b };
  return { x0: Math.min(a.x0, b.x0), y0: Math.min(a.y0, b.y0), x1: Math.max(a.x1, b.x1), y1: Math.max(a.y1, b.y1) };
}
function applyFrame(scene, frame, index) {
  if (frame.key) {
    const resized = frame.width !== scene.width || frame.height !== scene.height || frame.era !== scene.era;
    scene.width = frame.width;
    scene.height = frame.height;
    scene.era = frame.era;
    if (resized || scene.tiles.length !== frame.key.length) {
      scene.tiles = frame.key.slice();
      scene.dirty = "all";
    } else {
      const rect = diffRect(scene.tiles, frame.key, frame.width);
      scene.tiles.set(frame.key);
      if (rect) scene.dirty = union(scene.dirty, rect);
    }
  } else if (frame.patch) {
    pasteRect(scene.tiles, scene.width, frame.patch.rect, frame.patch.tiles);
    scene.dirty = union(scene.dirty, frame.patch.rect);
  }
  if (frame.objects) scene.objects = frame.objects;
  scene.index = index;
}
function keyBefore(frames, index) {
  for (let i = Math.min(index, frames.length - 1); i > 0; i--) if (frames[i].key) return i;
  return 0;
}
function objectsAt(frames, index) {
  for (let i = index; i >= 0; i--) {
    const o = frames[i].objects;
    if (o) return o;
  }
  return EMPTY_OBJECTS;
}
function seek(frames, index, from) {
  const target = Math.max(0, Math.min(index, frames.length - 1));
  const key = keyBefore(frames, target);
  let scene;
  if (from && from.index <= target && from.index >= key) {
    scene = from;
  } else {
    const k = frames[key];
    scene = { index: key, width: k.width, height: k.height, era: k.era, tiles: k.key.slice(), objects: objectsAt(frames, key), dirty: "all" };
  }
  for (let i = scene.index + 1; i <= target; i++) applyFrame(scene, frames[i], i);
  return scene;
}
function sampleFrames(count, max) {
  if (count <= 0) return [];
  if (max >= count) return Array.from({ length: count }, (_, i) => i);
  if (max < 2) return [count - 1];
  const out = [];
  for (let k = 0; k < max; k++) {
    const i = Math.round(k * (count - 1) / (max - 1));
    if (out[out.length - 1] !== i) out.push(i);
  }
  return out;
}
function extent(frames) {
  let width = 0, height = 0;
  for (const f of frames) {
    if (f.width > width) width = f.width;
    if (f.height > height) height = f.height;
  }
  return { width, height };
}
function formatDuration(ms) {
  const s = Math.round(ms / 1e3);
  if (s < 60) return `${s} s`;
  const m = Math.round(s / 60);
  if (m < 60) return `${m} min`;
  return `${Math.floor(m / 60)} h ${m % 60} min`;
}
function formatBytes(n) {
  if (n < 1024) return `${n} B`;
  if (n < 1024 * 1024) return `${(n / 1024).toFixed(0)} KB`;
  return `${(n / 1024 / 1024).toFixed(1)} MB`;
}
function frameBytes(f) {
  let n = 64;
  if (f.key) n += f.key.byteLength;
  if (f.patch) n += f.patch.tiles.byteLength;
  if (f.objects) n += f.objects.units.byteLength + f.objects.doodads.byteLength + f.objects.sprites.byteLength + f.objects.locations.byteLength;
  return n;
}

// export.ts
var tick = () => new Promise((resolve) => setTimeout(resolve, 0));
var sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms));
async function play(stage, plan, signal, each) {
  let scene = null;
  for (let k = 0; k < plan.indices.length; k++) {
    if (signal.aborted) throw new DOMException("Cancelled", "AbortError");
    const i = plan.indices[k];
    scene = seek(plan.frames, i, scene);
    const frame = plan.frames[i];
    stage.draw(scene, { ...plan.options, highlight: plan.options.highlight ? frame.area : null });
    await each(k);
  }
}
function gifDelay(fps) {
  return Math.max(20, Math.round(100 / fps) * 10);
}
async function exportGif(stage, plan, progress, signal) {
  const { width, height } = stage.canvas;
  const ctx = stage.canvas.getContext("2d", { willReadFrequently: true });
  const pixels = () => ctx.getImageData(0, 0, width, height).data;
  let scene = seek(plan.frames, plan.indices[0], null);
  stage.draw(scene, { ...plan.options, highlight: null });
  const first = pixels().slice();
  scene = seek(plan.frames, plan.indices[plan.indices.length - 1], null);
  stage.draw(scene, { ...plan.options, highlight: null });
  const last = pixels();
  const both = new Uint8ClampedArray(first.length * 2);
  both.set(first);
  both.set(last, first.length);
  const palette = (0, import_gifenc.quantize)(both, 255);
  const transparent = palette.length;
  palette.push([0, 0, 0]);
  const gif = (0, import_gifenc.GIFEncoder)();
  const delay = gifDelay(plan.fps);
  let shown = null;
  await play(stage, plan, signal, async (k) => {
    const index = (0, import_gifenc.applyPalette)(pixels(), palette);
    const lastFrame = k === plan.indices.length - 1;
    const frameDelay = lastFrame ? Math.max(delay, Math.round(plan.hold * 100) * 10) : delay;
    if (shown) {
      const out = new Uint8Array(index.length);
      for (let p = 0; p < index.length; p++) {
        if (index[p] === shown[p]) out[p] = transparent;
        else {
          out[p] = index[p];
          shown[p] = index[p];
        }
      }
      gif.writeFrame(out, width, height, { transparent: true, transparentIndex: transparent, delay: frameDelay, dispose: 1 });
    } else {
      gif.writeFrame(index, width, height, { palette, delay: frameDelay, dispose: 1 });
      shown = index;
    }
    progress(k + 1, plan.indices.length);
    await tick();
  });
  gif.finish();
  return new Blob([gif.bytes()], { type: "image/gif" });
}
function videoType() {
  if (typeof MediaRecorder === "undefined") return null;
  for (const type of ["video/webm;codecs=vp9", "video/webm;codecs=vp8", "video/webm", "video/mp4"]) {
    if (MediaRecorder.isTypeSupported(type)) return type;
  }
  return null;
}
async function exportVideo(stage, plan, progress, signal) {
  const type = videoType();
  if (!type) throw new Error("This browser cannot record video.");
  const stream = stage.canvas.captureStream(0);
  const track = stream.getVideoTracks()[0];
  const recorder = new MediaRecorder(stream, { mimeType: type, videoBitsPerSecond: 8e6 });
  const chunks = [];
  recorder.ondataavailable = (e) => {
    if (e.data.size > 0) chunks.push(e.data);
  };
  const stopped = new Promise((resolve) => {
    recorder.onstop = () => resolve();
  });
  recorder.start();
  const step = 1e3 / plan.fps;
  let next = performance.now();
  try {
    await play(stage, plan, signal, async (k) => {
      track.requestFrame?.();
      progress(k + 1, plan.indices.length);
      next += step;
      await sleep(Math.max(0, next - performance.now()));
    });
    const holdUntil = performance.now() + plan.hold * 1e3;
    while (performance.now() < holdUntil) {
      if (signal.aborted) throw new DOMException("Cancelled", "AbortError");
      track.requestFrame?.();
      await sleep(Math.min(250, holdUntil - performance.now()));
    }
  } finally {
    recorder.stop();
    await stopped;
    for (const t of stream.getTracks()) t.stop();
  }
  return new Blob(chunks, { type: type.split(";")[0] });
}

// ko.ts
var KO = {
  " (paused)": " (\uC77C\uC2DC \uC815\uC9C0\uB428)",
  " (recording)": " (\uB179\uD654 \uC911)",
  "A long recording makes a large GIF. Fewer frames or a smaller size keep it easy to share.": "\uB179\uD654\uAC00 \uAE38\uBA74 GIF\uB3C4 \uCEE4\uC9D1\uB2C8\uB2E4. \uD504\uB808\uC784 \uC218\uB97C \uC904\uC774\uAC70\uB098 \uD06C\uAE30\uB97C \uC791\uAC8C \uD558\uBA74 \uACF5\uC720\uD558\uAE30 \uC27D\uC2B5\uB2C8\uB2E4.",
  "Animated GIF": "\uC6C0\uC9C1\uC774\uB294 GIF",
  "Box around each change": "\uBC14\uB010 \uACF3\uC5D0 \uD14C\uB450\uB9AC",
  "Cancel": "\uCDE8\uC18C",
  "Delete all recordings\u2026": "\uB179\uD654 \uBAA8\uB450 \uC0AD\uC81C\u2026",
  "Delete all": "\uBAA8\uB450 \uC0AD\uC81C",
  "Delete every recording? This cannot be undone.": "\uB179\uD654\uB97C \uBAA8\uB450 \uC0AD\uC81C\uD560\uAE4C\uC694? \uB418\uB3CC\uB9B4 \uC218 \uC5C6\uC2B5\uB2C8\uB2E4.",
  "Delete the recording of \u201C{name}\u201D? This cannot be undone.": "\u201C{name}\u201D \uB179\uD654\uB97C \uC0AD\uC81C\uD560\uAE4C\uC694? \uB418\uB3CC\uB9B4 \uC218 \uC5C6\uC2B5\uB2C8\uB2E4.",
  "Delete": "\uC0AD\uC81C",
  "Delete\u2026": "\uC0AD\uC81C\u2026",
  "Doodads and sprites": "\uC7A5\uC2DD\uBB3C\uACFC \uC2A4\uD504\uB77C\uC774\uD2B8",
  "Encoding frames": "\uD504\uB808\uC784 \uC778\uCF54\uB529 \uC911",
  "Export Timelapse": "\uD0C0\uC784\uB7A9\uC2A4 \uB0B4\uBCF4\uB0B4\uAE30",
  "Export": "\uB0B4\uBCF4\uB0B4\uAE30",
  "Export\u2026": "\uB0B4\uBCF4\uB0B4\uAE30\u2026",
  "First frame": "\uCCAB \uD504\uB808\uC784",
  "Format": "\uD615\uC2DD",
  "Frames at most": "\uCD5C\uB300 \uD504\uB808\uC784 \uC218",
  "Frames per second": "\uCD08\uB2F9 \uD504\uB808\uC784",
  "Hold the end (seconds)": "\uB9C8\uC9C0\uB9C9 \uC7A5\uBA74 \uC720\uC9C0 (\uCD08)",
  "Last frame": "\uB9C8\uC9C0\uB9C9 \uD504\uB808\uC784",
  "Loading the recording\u2026": "\uB179\uD654\uB97C \uBD88\uB7EC\uC624\uB294 \uC911\u2026",
  "Locations": "\uB85C\uCF00\uC774\uC158",
  "Map settings": "\uB9F5 \uC124\uC815",
  "Not saved.": "\uC800\uC7A5\uD558\uC9C0 \uC54A\uC558\uC2B5\uB2C8\uB2E4.",
  "Nothing recorded yet. Change the map and every change becomes a frame here.": "\uC544\uC9C1 \uB179\uD654\uB41C \uAC83\uC774 \uC5C6\uC2B5\uB2C8\uB2E4. \uB9F5\uC744 \uACE0\uCE58\uBA74 \uBC14\uB010 \uB0B4\uC6A9\uB9C8\uB2E4 \uC5EC\uAE30\uC5D0 \uD504\uB808\uC784\uC774 \uD558\uB098\uC529 \uC0DD\uAE41\uB2C8\uB2E4.",
  "Off, nothing is recorded until you press Record in the Timelapse panel.": "\uB044\uBA74 \uD0C0\uC784\uB7A9\uC2A4 \uD328\uB110\uC5D0\uC11C \uB179\uD654\uB97C \uB204\uB974\uAE30 \uC804\uAE4C\uC9C0 \uC544\uBB34\uAC83\uB3C4 \uB179\uD654\uD558\uC9C0 \uC54A\uC2B5\uB2C8\uB2E4.",
  "Pause recording": "\uB179\uD654 \uC77C\uC2DC \uC815\uC9C0",
  "Pause": "\uC77C\uC2DC \uC815\uC9C0",
  "Play": "\uC7AC\uC0DD",
  "Record every map while I edit it": "\uD3B8\uC9D1\uD558\uB294 \uBAA8\uB4E0 \uB9F5\uC744 \uB179\uD654",
  "Record": "\uB179\uD654",
  "Recording is off. Press Record to start recording the map in front.": "\uB179\uD654\uAC00 \uAEBC\uC838 \uC788\uC2B5\uB2C8\uB2E4. \uB179\uD654\uB97C \uB204\uB974\uBA74 \uC9C0\uAE08 \uBCF4\uACE0 \uC788\uB294 \uB9F5\uC744 \uB179\uD654\uD569\uB2C8\uB2E4.",
  "Recording the video": "\uB3D9\uC601\uC0C1 \uB179\uD654 \uC911",
  "Recording": "\uB179\uD654",
  "Recordings to keep": "\uBCF4\uAD00\uD560 \uB179\uD654 \uC218",
  "Redo {label}": "\uB2E4\uC2DC \uC2E4\uD589: {label}",
  "Resume recording": "\uB179\uD654 \uACC4\uC18D",
  "Size": "\uD06C\uAE30",
  "Someone else's edits": "\uB2E4\uB978 \uC0AC\uB78C\uC758 \uD3B8\uC9D1",
  "Start recording the map in front": "\uC9C0\uAE08 \uBCF4\uACE0 \uC788\uB294 \uB9F5 \uB179\uD654 \uC2DC\uC791",
  "Stop": "\uC815\uC9C0",
  "Stopped.": "\uC815\uC9C0\uD588\uC2B5\uB2C8\uB2E4.",
  "Storage": "\uC800\uC7A5\uC18C",
  "The browser's storage is not available, so recordings last only until the page is reloaded.": "\uBE0C\uB77C\uC6B0\uC800 \uC800\uC7A5\uC18C\uB97C \uC4F8 \uC218 \uC5C6\uC5B4 \uB179\uD654\uB294 \uD398\uC774\uC9C0\uB97C \uC0C8\uB85C \uACE0\uCE60 \uB54C\uAE4C\uC9C0\uB9CC \uB0A8\uC2B5\uB2C8\uB2E4.",
  "The export failed: {error}": "\uB0B4\uBCF4\uB0B4\uC9C0 \uBABB\uD588\uC2B5\uB2C8\uB2E4: {error}",
  "The map when recording began": "\uB179\uD654\uB97C \uC2DC\uC791\uD560 \uB54C\uC758 \uB9F5",
  "The recording goes on for this session, but it will not be there after a reload.": "\uC774\uBC88 \uC138\uC158 \uB3D9\uC548\uC740 \uACC4\uC18D \uB179\uD654\uD558\uC9C0\uB9CC, \uC0C8\uB85C \uACE0\uCE58\uBA74 \uB0A8\uC9C0 \uC54A\uC2B5\uB2C8\uB2E4.",
  "The tileset graphics this recording needs are not installed (Help \u25B8 Game Data\u2026).": "\uC774 \uB179\uD654\uC5D0 \uD544\uC694\uD55C \uD0C0\uC77C\uC14B \uADF8\uB798\uD53D\uC774 \uC124\uCE58\uB418\uC5B4 \uC788\uC9C0 \uC54A\uC2B5\uB2C8\uB2E4 (\uB3C4\uC6C0\uB9D0 \u25B8 \uAC8C\uC784 \uB370\uC774\uD130\u2026).",
  "Timelapse could not save to the browser's storage": "\uD0C0\uC784\uB7A9\uC2A4\uB97C \uBE0C\uB77C\uC6B0\uC800 \uC800\uC7A5\uC18C\uC5D0 \uC800\uC7A5\uD558\uC9C0 \uBABB\uD588\uC2B5\uB2C8\uB2E4",
  "Timelapse is paused for this map. Click to open it.": "\uC774 \uB9F5\uC758 \uD0C0\uC784\uB7A9\uC2A4\uAC00 \uC77C\uC2DC \uC815\uC9C0\uB418\uC5C8\uC2B5\uB2C8\uB2E4. \uB20C\uB7EC\uC11C \uC5EC\uC138\uC694.",
  "Timelapse is recording this map. Click to watch it.": "\uD0C0\uC784\uB7A9\uC2A4\uAC00 \uC774 \uB9F5\uC744 \uB179\uD654\uD558\uACE0 \uC788\uC2B5\uB2C8\uB2E4. \uB20C\uB7EC\uC11C \uBCF4\uC138\uC694.",
  "Timelapse paused": "\uD0C0\uC784\uB7A9\uC2A4 \uC77C\uC2DC \uC815\uC9C0",
  "Timelapse saved": "\uD0C0\uC784\uB7A9\uC2A4\uB97C \uC800\uC7A5\uD588\uC2B5\uB2C8\uB2E4",
  "Timelapse": "\uD0C0\uC784\uB7A9\uC2A4",
  "Timelapse\u2026": "\uD0C0\uC784\uB7A9\uC2A4\u2026",
  "Undo {label}": "\uC2E4\uD589 \uCDE8\uC18C: {label}",
  "Units": "\uC720\uB2DB",
  "Untitled map": "\uC81C\uBAA9 \uC5C6\uB294 \uB9F5",
  "Video (WebM)": "\uB3D9\uC601\uC0C1 (WebM)",
  "Video (not available in this browser)": "\uB3D9\uC601\uC0C1 (\uC774 \uBE0C\uB77C\uC6B0\uC800\uC5D0\uC11C\uB294 \uC4F8 \uC218 \uC5C6\uC74C)",
  "When a new recording starts, the oldest beyond this number are deleted.": "\uC0C8 \uB179\uD654\uB97C \uC2DC\uC791\uD558\uBA74 \uC774 \uC218\uB97C \uB118\uB294 \uC624\uB798\uB41C \uB179\uD654\uBD80\uD130 \uC0AD\uC81C\uD569\uB2C8\uB2E4.",
  "{frames} of {total} frames, {seconds} s long": "\uD504\uB808\uC784 {total}\uAC1C \uC911 {frames}\uAC1C, {seconds}\uCD08",
  "{label} \u2014 {d} of {n}": "{label} \u2014 {n}\uAC1C \uC911 {d}\uAC1C",
  "{n, plural, one {# recording} other {# recordings}}, about {size}, kept in this browser.": "\uB179\uD654 {n}\uAC1C, \uC57D {size}, \uC774 \uBE0C\uB77C\uC6B0\uC800\uC5D0 \uBCF4\uAD00 \uC911.",
  "{n} fps": "{n} fps",
  "{n} px per tile \u2014 {w} \xD7 {h}": "\uD0C0\uC77C\uB2F9 {n}px \u2014 {w} \xD7 {h}",
  "{size} \u2014 choose where to save it": "{size} \u2014 \uC800\uC7A5\uD560 \uACF3\uC744 \uACE0\uB974\uC138\uC694",
  "{time} in": "{time} \uACBD\uACFC",
  "{time} of editing \xB7 {size}": "\uD3B8\uC9D1 {time} \xB7 {size}",
  "\u25CF {n, plural, one {# frame} other {# frames}}": "\u25CF \uD504\uB808\uC784 {n}\uAC1C"
};

// library.ts
var DB = "scmjs-timelapse";
var INFO = "recordings";
var FRAMES = "frames";
function request(req) {
  return new Promise((resolve, reject) => {
    req.onsuccess = () => resolve(req.result);
    req.onerror = () => reject(req.error);
  });
}
function done(tx) {
  return new Promise((resolve, reject) => {
    tx.oncomplete = () => resolve();
    tx.onerror = () => reject(tx.error);
    tx.onabort = () => reject(tx.error ?? new Error("aborted"));
  });
}
var Library = class _Library {
  db;
  constructor(db) {
    this.db = db;
  }
  static async open() {
    if (typeof indexedDB === "undefined") return null;
    try {
      const req = indexedDB.open(DB, 1);
      req.onupgradeneeded = () => {
        const db = req.result;
        if (!db.objectStoreNames.contains(INFO)) db.createObjectStore(INFO, { keyPath: "id" });
        if (!db.objectStoreNames.contains(FRAMES)) db.createObjectStore(FRAMES);
      };
      return new _Library(await request(req));
    } catch {
      return null;
    }
  }
  /** Every recording's summary, newest first. */
  async list() {
    const all = await request(this.db.transaction(INFO).objectStore(INFO).getAll());
    return all.sort((a, b) => b.updated - a.updated);
  }
  async frames(id) {
    const range = IDBKeyRange.bound([id, 0], [id, Number.MAX_SAFE_INTEGER]);
    return request(this.db.transaction(FRAMES).objectStore(FRAMES).getAll(range));
  }
  async saveInfo(info) {
    const tx = this.db.transaction(INFO, "readwrite");
    tx.objectStore(INFO).put(info);
    await done(tx);
  }
  /** One frame and the summary that now counts it, in one transaction, so the two never disagree. */
  async addFrame(info, index, frame) {
    const tx = this.db.transaction([FRAMES, INFO], "readwrite");
    tx.objectStore(FRAMES).put(frame, [info.id, index]);
    tx.objectStore(INFO).put({ ...info });
    await done(tx);
  }
  async remove(id) {
    const tx = this.db.transaction([INFO, FRAMES], "readwrite");
    tx.objectStore(INFO).delete(id);
    tx.objectStore(FRAMES).delete(IDBKeyRange.bound([id, 0], [id, Number.MAX_SAFE_INTEGER]));
    await done(tx);
  }
  close() {
    this.db.close();
  }
};
function newId() {
  return `${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 8)}`;
}

// render.ts
var TILESETS = ["badlands", "platform", "install", "ashworld", "jungle", "desert", "ice", "twilight"];
var BACKGROUND = "#000";
var HIGHLIGHT = "rgba(244, 208, 138, 0.95)";
function canvas(width, height) {
  const c = document.createElement("canvas");
  c.width = Math.max(1, width);
  c.height = Math.max(1, height);
  return c;
}
function objectClip(scene, options) {
  const o = scene.objects;
  const units = [];
  if (options.units) for (let i = 0; i < o.units.length; i += UNIT_STRIDE) units.push({ unitId: o.units[i], owner: o.units[i + 1], x: o.units[i + 2], y: o.units[i + 3] });
  const doodads = [];
  const sprites = [];
  if (options.sprites) {
    for (let i = 0; i < o.doodads.length; i += DOODAD_STRIDE) doodads.push({ doodadId: o.doodads[i], owner: o.doodads[i + 1], x: o.doodads[i + 2], y: o.doodads[i + 3], disabled: 0 });
    for (let i = 0; i < o.sprites.length; i += SPRITE_STRIDE) sprites.push({ spriteId: o.sprites[i], flags: o.sprites[i + 1], owner: o.sprites[i + 2], x: o.sprites[i + 3], y: o.sprites[i + 4], unused: 0 });
  }
  const locations = [];
  if (options.locations) {
    for (let i = 0; i < o.locations.length; i += LOCATION_STRIDE) {
      locations.push({ left: o.locations[i], top: o.locations[i + 1], right: o.locations[i + 2], bottom: o.locations[i + 3], elevationFlags: 0, name: "" });
    }
  }
  return {
    width: scene.width,
    height: scene.height,
    era: scene.era,
    tiles: null,
    ground: null,
    units,
    doodads,
    sprites,
    locations,
    fog: null
  };
}
var Stage = class {
  /** What is shown and exported: the whole extent, the map in its top-left corner, black around it. */
  canvas;
  ctx;
  terrain = null;
  terrainEra = -1;
  api;
  ppt;
  extent;
  /** `readback` for a stage whose pixels are read after every frame (the GIF export). */
  constructor(api, ppt, extent2, readback = false) {
    this.api = api;
    this.ppt = ppt;
    this.extent = extent2;
    this.canvas = canvas(extent2.width * ppt, extent2.height * ppt);
    this.ctx = this.canvas.getContext("2d", { willReadFrequently: readback });
  }
  /** Fetch the tileset graphics a scene on `era` is drawn with. False when they were never extracted. */
  async prepare(era) {
    const id = TILESETS[era & 7];
    return await this.api.tileset.load(id) && (await this.api.graphics.load()).tileset;
  }
  /** Draw `scene` and clear its dirty mark. False when its tileset's graphics are not in memory. */
  draw(scene, options) {
    const { ppt } = this;
    const w = scene.width * ppt, h2 = scene.height * ppt;
    let full = scene.dirty === "all";
    if (!this.terrain || this.terrain.width !== w || this.terrain.height !== h2 || this.terrainEra !== scene.era) {
      this.terrain = canvas(w, h2);
      this.terrainEra = scene.era;
      full = true;
    }
    const rect = full ? { x0: 0, y0: 0, x1: scene.width, y1: scene.height } : scene.dirty;
    let ok = true;
    if (rect && rect.x1 > rect.x0 && rect.y1 > rect.y0) {
      const cells = cutRect(scene.tiles, scene.width, rect);
      const clip = {
        width: rect.x1 - rect.x0,
        height: rect.y1 - rect.y0,
        era: scene.era,
        tiles: cells,
        ground: cells,
        units: [],
        doodads: [],
        sprites: [],
        locations: [],
        fog: null
      };
      const image = this.api.graphics.renderClip(clip, {
        pixelsPerTile: ppt,
        parts: { terrain: true, doodads: true, units: false, sprites: false, locations: false, fog: false }
      });
      const t = this.terrain.getContext("2d");
      t.fillStyle = BACKGROUND;
      t.fillRect(rect.x0 * ppt, rect.y0 * ppt, (rect.x1 - rect.x0) * ppt, (rect.y1 - rect.y0) * ppt);
      if (image) t.drawImage(image.image, rect.x0 * ppt, rect.y0 * ppt);
      else ok = false;
    }
    scene.dirty = ok ? null : "all";
    const ctx = this.ctx;
    ctx.fillStyle = BACKGROUND;
    ctx.fillRect(0, 0, this.canvas.width, this.canvas.height);
    ctx.drawImage(this.terrain, 0, 0);
    if (options.units || options.sprites || options.locations) {
      const objects = this.api.graphics.renderClip(objectClip(scene, options), {
        pixelsPerTile: ppt,
        parts: { terrain: false, doodads: options.sprites, units: options.units, sprites: options.sprites, locations: options.locations, fog: false }
      });
      if (objects) ctx.drawImage(objects.image, 0, 0);
    }
    const hl = options.highlight;
    if (hl) {
      const pad = Math.max(1, Math.round(ppt / 2));
      ctx.strokeStyle = HIGHLIGHT;
      ctx.lineWidth = Math.max(1, Math.round(ppt / 3));
      ctx.strokeRect(hl.x0 * ppt - pad + 0.5, hl.y0 * ppt - pad + 0.5, (hl.x1 - hl.x0) * ppt + 2 * pad - 1, (hl.y1 - hl.y0) * ppt + 2 * pad - 1);
    }
    return ok;
  }
};
function fitPpt(extent2, target, max = 8) {
  return Math.max(1, Math.min(max, Math.floor(target / Math.max(1, extent2.width, extent2.height))));
}

// plugin.ts
var ICONS = {
  first: '<path d="M3 2h1.6v10H3zM12 2v10L5.5 7z"/>',
  last: '<path d="M9.4 2H11v10H9.4zM2 2v10l6.5-5z"/>',
  play: '<path d="M3.5 2v10l8.5-5z"/>',
  pause: '<path d="M3 2h3v10H3zM8 2h3v10H8z"/>'
};
function icon(name) {
  const svg = document.createElementNS("http://www.w3.org/2000/svg", "svg");
  svg.setAttribute("viewBox", "0 0 14 14");
  svg.setAttribute("width", "12");
  svg.setAttribute("height", "12");
  svg.setAttribute("fill", "currentColor");
  svg.setAttribute("aria-hidden", "true");
  svg.innerHTML = ICONS[name];
  return svg;
}
function h(tag, props = null, ...children) {
  const el = document.createElement(tag);
  if (props) {
    for (const [k, v] of Object.entries(props)) {
      if (v === void 0 || v === null || v === false) continue;
      if (k === "className") el.className = String(v);
      else if (k.startsWith("on") && typeof v === "function") el.addEventListener(k.slice(2).toLowerCase(), v);
      else if (k in el && typeof v !== "string") el[k] = v;
      else el.setAttribute(k, String(v));
    }
  }
  for (const c of children) if (c !== null && c !== void 0 && c !== false) el.append(typeof c === "string" ? document.createTextNode(c) : c);
  return el;
}
var STYLE = `
.tl { display: flex; flex-direction: column; flex: 1; min-height: 0; gap: 6px; font-size: var(--fs-sm, 11.5px); }
.tl .tl-bar { display: flex; gap: 4px; align-items: center; }
.tl .tl-bar .select { flex: 1; min-width: 0; }
.tl .tl-stage { position: relative; flex: 1; min-height: 160px; display: flex; align-items: center; justify-content: center; background: #000; border: 1px solid var(--border, #2c3341); border-radius: var(--radius, 3px); overflow: hidden; }
.tl .tl-stage canvas { position: absolute; inset: 0; width: 100%; height: 100%; object-fit: contain; }
.tl .tl-empty { padding: 18px; text-align: center; color: var(--text-faint, #5d6675); line-height: 1.5; max-width: 320px; }
.tl .tl-controls { display: flex; gap: 6px; align-items: center; }
.tl .tl-controls input[type=range] { flex: 1; min-width: 0; accent-color: var(--teal, #4fd1c5); }
.tl .tl-controls .tl-count { color: var(--text-dim, #99a2b3); font-variant-numeric: tabular-nums; white-space: nowrap; }
.tl .tl-controls .btn { min-width: 30px; padding: 0 6px; }
.tl .tl-caption { display: flex; gap: 8px; min-height: 16px; color: var(--text-dim, #99a2b3); white-space: nowrap; overflow: hidden; }
.tl .tl-caption b { color: var(--text, #dde2ea); font-weight: normal; overflow: hidden; text-overflow: ellipsis; }
.tl .tl-caption .grow { flex: 1; }
.tl .tl-opts { display: flex; flex-wrap: wrap; gap: 2px 12px; align-items: center; }
.tl .tl-opts .check { height: 18px; }
.tl .tl-opts .select { width: auto; }
.tl .tl-foot { display: flex; gap: 4px; align-items: center; }
.tl .tl-foot .grow { flex: 1; }
.tl .tl-foot .btn { padding: 0 8px; }
.tl .tl-dot { display: inline-block; width: 7px; height: 7px; border-radius: 50%; background: #e05252; margin-right: 5px; vertical-align: 0; }
.tl-dlg { display: flex; flex-direction: column; gap: 10px; }
.tl-dlg .tl-estimate { color: var(--text-dim, #99a2b3); }
`;
var DEFAULTS = {
  auto: true,
  keep: 30,
  fps: 12,
  units: true,
  sprites: true,
  locations: false,
  highlight: true,
  exportFormat: "gif",
  exportPpt: 0,
  exportFps: 12,
  exportMax: 300,
  exportHold: 2,
  open: false
};
function mapState(api) {
  const scn = api.document.scenario();
  if (!scn) return null;
  return {
    width: scn.width,
    height: scn.height,
    era: scn.era & 7,
    tiles: scn.tiles,
    units: scn.units,
    doodads: scn.doodads,
    sprites: scn.sprites,
    locations: scn.locations
  };
}
var Timelapse = class {
  settings;
  library = null;
  /** Whether IndexedDB refused; recordings are then kept for this session only. */
  memoryOnly = false;
  live = /* @__PURE__ */ new Map();
  pending = /* @__PURE__ */ new Map();
  /** The recordings' summaries, newest first, kept in step with the library. */
  infos = [];
  status = null;
  panel = null;
  player = null;
  style;
  api;
  constructor(api) {
    this.api = api;
    this.settings = { ...DEFAULTS, ...api.storage.get("settings", {}) };
    this.style = h("style", null, STYLE);
    document.head.append(this.style);
  }
  t = (text, params) => this.api.i18n.t(text, params);
  save(patch) {
    this.settings = { ...this.settings, ...patch };
    this.api.storage.set("settings", this.settings);
  }
  async init() {
    this.library = await Library.open();
    this.memoryOnly = this.library === null;
    if (this.library) {
      try {
        this.infos = await this.library.list();
      } catch (err) {
        this.api.log("Could not read the recordings", err);
      }
    }
    const id = this.api.document.id();
    if (id !== null) this.prepare(id);
    this.player?.refreshList();
  }
  /* ── Following the editor ── */
  /** Note the map in front as it stands, so its first change has a frame to start from. */
  prepare(doc) {
    const state = mapState(this.api);
    if (!state || this.live.has(doc)) return;
    const pending = { start: Recorder.start(state, Date.now()), continueWith: null };
    this.pending.set(doc, pending);
    const fileName = this.api.document.info()?.fileName ?? null;
    if (fileName && this.library) void this.findContinuation(doc, pending, fileName);
  }
  /** The newest recording of this file, if the map is exactly as it left it. */
  async findContinuation(doc, pending, fileName) {
    const info = this.infos.find((i) => i.fileName === fileName && ![...this.live.values()].some((l) => l.info.id === i.id));
    if (!info || !this.library) return;
    try {
      const frames = await this.library.frames(info.id);
      if (frames.length === 0 || this.pending.get(doc) !== pending) return;
      const end = seek(frames, frames.length - 1, null);
      if (sameSnapshot(end, pending.start.recorder.state())) pending.continueWith = { info, frames };
    } catch (err) {
      this.api.log("Could not read a recording", err);
    }
  }
  onDocument(e) {
    const open = new Set(this.api.document.list().map((d) => d.id));
    for (const [doc, live] of this.live) if (!open.has(doc)) {
      this.live.delete(doc);
      if (this.player?.showing === live.info.id) this.player.refreshList();
    }
    for (const doc of [...this.pending.keys()]) if (!open.has(doc)) this.pending.delete(doc);
    if (e.id !== null) {
      const live = this.live.get(e.id);
      if (live) live.recorder.touch(Date.now());
      else if (e.reason !== "switch" || !this.pending.has(e.id)) this.prepare(e.id);
    }
    this.updateStatus();
    this.player?.followDocument();
  }
  onFile() {
    const doc = this.api.document.id();
    const live = doc === null ? void 0 : this.live.get(doc);
    const fileName = this.api.document.info()?.fileName ?? null;
    if (live && fileName && live.info.fileName !== fileName) {
      live.info.fileName = fileName;
      void this.library?.saveInfo({ ...live.info }).catch((err) => this.api.log("Could not save a recording", err));
    }
  }
  onCommit(e) {
    if (e.id === null) return;
    let live = this.live.get(e.id);
    if (!live) {
      if (!this.settings.auto) return;
      live = this.begin(e.id);
      if (!live) return;
    }
    if (live.paused) return;
    const state = mapState(this.api);
    if (!state) return;
    const frame = live.recorder.take(state, e, Date.now());
    if (frame) this.append(live, frame);
  }
  /** Start recording the map `doc` — from its pending start, or carrying on its file's recording. */
  begin(doc) {
    let pending = this.pending.get(doc);
    if (!pending) {
      this.prepare(doc);
      pending = this.pending.get(doc);
      if (!pending) return null;
    }
    this.pending.delete(doc);
    const now = Date.now();
    let live;
    if (pending.continueWith) {
      const { info, frames } = pending.continueWith;
      const end = seek(frames, frames.length - 1, null);
      live = { doc, info: { ...info }, recorder: new Recorder(end, frames.length, frames[frames.length - 1].t, now), frames, paused: false };
    } else {
      const docInfo = this.api.document.info();
      const info = {
        id: newId(),
        mapName: docInfo?.name || this.t("Untitled map"),
        fileName: docInfo?.fileName ?? null,
        created: now,
        updated: now,
        frames: 0,
        elapsed: 0,
        bytes: 0
      };
      live = { doc, info, recorder: pending.start.recorder, frames: [], paused: false };
      this.append(live, pending.start.frame);
      void this.prune();
    }
    this.live.set(doc, live);
    this.infos = [live.info, ...this.infos.filter((i) => i.id !== live.info.id)];
    this.updateStatus();
    this.player?.refreshList();
    return live;
  }
  append(live, frame) {
    const index = live.frames.length;
    live.frames.push(frame);
    live.info.frames = live.frames.length;
    live.info.elapsed = frame.t;
    live.info.updated = Date.now();
    live.info.bytes += frameBytes(frame);
    if (this.library) {
      this.library.addFrame(live.info, index, frame).catch((err) => {
        this.api.log("Could not store a frame", err);
        if (!this.memoryOnly) {
          this.memoryOnly = true;
          this.api.ui.toast({ kind: "warn", title: this.t("Timelapse could not save to the browser's storage"), detail: this.t("The recording goes on for this session, but it will not be there after a reload.") });
        }
      });
    }
    this.updateStatus();
    this.player?.framesAdded(live);
  }
  /** Keep the newest `keep` recordings; the ones being recorded always stay. */
  async prune() {
    const liveIds = new Set([...this.live.values()].map((l) => l.info.id));
    const extra = this.infos.filter((i) => !liveIds.has(i.id)).slice(Math.max(0, this.settings.keep - liveIds.size - 1));
    for (const info of extra) await this.remove(info.id);
  }
  async remove(id) {
    for (const [doc, live] of this.live) {
      if (live.info.id === id) {
        this.live.delete(doc);
        this.prepare(doc);
      }
    }
    this.infos = this.infos.filter((i) => i.id !== id);
    try {
      await this.library?.remove(id);
    } catch (err) {
      this.api.log("Could not delete a recording", err);
    }
    this.updateStatus();
    this.player?.refreshList();
  }
  async removeAll() {
    for (const info of [...this.infos]) await this.remove(info.id);
  }
  /** The front map's recording, if it has one. */
  front() {
    const doc = this.api.document.id();
    return doc === null ? null : this.live.get(doc) ?? null;
  }
  /** Record, pause or resume the map in front. */
  toggleRecording() {
    const doc = this.api.document.id();
    if (doc === null) return;
    const live = this.live.get(doc);
    if (!live) this.begin(doc);
    else {
      live.paused = !live.paused;
      if (!live.paused) live.recorder.touch(Date.now());
    }
    this.updateStatus();
    this.player?.updateFooter();
  }
  updateStatus() {
    const live = this.front();
    if (!live) {
      this.status?.remove();
      this.status = null;
      return;
    }
    const text = live.paused ? this.t("Timelapse paused") : this.t("\u25CF {n, plural, one {# frame} other {# frames}}", { n: live.frames.length });
    const title = live.paused ? this.t("Timelapse is paused for this map. Click to open it.") : this.t("Timelapse is recording this map. Click to watch it.");
    if (this.status?.isShown()) this.status.set({ text, title });
    else this.status = this.api.ui.statusItem({ text, title, onClick: () => this.openPanel() });
  }
  /* ── The panel ── */
  openPanel() {
    if (this.panel?.isOpen()) return;
    this.save({ open: true });
    this.panel = this.api.ui.panel({
      title: this.t("Timelapse"),
      width: 480,
      height: 460,
      resizable: true,
      mount: (body) => {
        this.player = new Player(this);
        body.append(this.player.root);
        this.player.refreshList();
        return () => {
          this.player?.stop();
          this.player = null;
        };
      },
      onClose: () => {
        this.panel = null;
        if (!this.disposed) this.save({ open: false });
      }
    });
  }
  togglePanel() {
    if (this.panel?.isOpen()) this.panel.close();
    else this.openPanel();
  }
  /** The language changed: everything on screen was built in the old one. */
  relabel() {
    this.updateStatus();
    if (this.panel?.isOpen()) {
      const showing = this.player?.showing ?? null;
      this.panel.close();
      this.openPanel();
      if (showing) void this.player?.show(showing);
    }
  }
  /* ── Export ── */
  exportDialog(info, frames) {
    const { api, t } = this;
    const w = api.ui.widgets;
    const s = this.settings;
    const size = extent(frames);
    const autoPpt = fitPpt(size, 512);
    const video = videoType();
    const format = w.select([
      { value: "gif", label: t("Animated GIF") },
      { value: "video", label: video ? t("Video (WebM)") : t("Video (not available in this browser)"), disabled: !video }
    ], { value: video ? s.exportFormat : "gif" });
    const ppt = w.select([1, 2, 3, 4, 6, 8, 12, 16].map((n) => ({
      value: n,
      label: t("{n} px per tile \u2014 {w} \xD7 {h}", { n, w: size.width * n, h: size.height * n })
    })), { value: s.exportPpt || autoPpt });
    const fps = w.number({ value: s.exportFps, min: 1, max: 50, step: 1 });
    const max = w.number({ value: Math.min(s.exportMax, frames.length), min: 2, max: frames.length, step: 1 });
    const hold = w.number({ value: s.exportHold, min: 0, max: 30, step: 0.5 });
    const units = w.checkbox(t("Units"), { value: s.units });
    const sprites = w.checkbox(t("Doodads and sprites"), { value: s.sprites });
    const locations = w.checkbox(t("Locations"), { value: s.locations });
    const highlight = w.checkbox(t("Box around each change"), { value: s.highlight });
    const estimate = h("div", { className: "tl-estimate" });
    const status = w.statusLine();
    const plan = () => {
      const n = Math.max(2, Math.min(frames.length, Math.round(Number(max.value) || frames.length)));
      return {
        frames,
        indices: sampleFrames(frames.length, n),
        fps: Math.max(1, Math.min(50, Number(fps.value) || 12)),
        hold: Math.max(0, Number(hold.value) || 0),
        options: { units: units.input.checked, sprites: sprites.input.checked, locations: locations.input.checked, highlight: highlight.input.checked }
      };
    };
    const updateEstimate = () => {
      const p = plan();
      const perFrame = format.value === "gif" ? gifDelay(p.fps) / 1e3 : 1 / p.fps;
      const seconds = p.indices.length * perFrame + p.hold;
      estimate.textContent = t("{frames} of {total} frames, {seconds} s long", { frames: p.indices.length, total: frames.length, seconds: seconds.toFixed(1) });
    };
    for (const el of [format, ppt, fps, max, hold]) el.addEventListener("input", updateEstimate);
    updateEstimate();
    let abort = null;
    api.ui.dialog({
      title: t("Export Timelapse"),
      size: "md",
      mount: (body) => {
        body.append(h(
          "div",
          { className: "tl-dlg" },
          w.form([
            { label: t("Format"), field: format },
            { label: t("Size"), field: ppt },
            { label: t("Frames at most"), field: max },
            { label: t("Frames per second"), field: fps },
            { label: t("Hold the end (seconds)"), field: hold }
          ]),
          w.row(units, sprites, locations, highlight),
          estimate,
          format.value === "gif" && frames.length > 600 && w.hint(t("A long recording makes a large GIF. Fewer frames or a smaller size keep it easy to share.")),
          status
        ));
        return () => abort?.abort();
      },
      buttons: [
        { label: t("Cancel") },
        {
          label: t("Export"),
          primary: true,
          run: async () => {
            const p = plan();
            const kind = format.value === "video" ? "video" : "gif";
            this.save({
              exportFormat: kind,
              exportPpt: Number(ppt.value) === autoPpt ? 0 : Number(ppt.value),
              exportFps: p.fps,
              exportMax: p.indices.length,
              exportHold: p.hold
            });
            const stage = new Stage(api, Number(ppt.value), size, kind === "gif");
            for (const era of new Set(frames.map((f) => f.era))) {
              if (!await stage.prepare(era)) {
                status.set(t("The tileset graphics this recording needs are not installed (Help \u25B8 Game Data\u2026)."), "error");
                return false;
              }
            }
            abort = new AbortController();
            const signal = abort.signal;
            status.cancel(() => abort?.abort(), t("Stop"));
            const label = kind === "gif" ? t("Encoding frames") : t("Recording the video");
            try {
              const blob = kind === "gif" ? await exportGif(stage, p, (d, n) => status.progress(t("{label} \u2014 {d} of {n}", { label, d, n }), d / n), signal) : await exportVideo(stage, p, (d, n) => status.progress(t("{label} \u2014 {d} of {n}", { label, d, n }), d / n), signal);
              status.cancel(null);
              const ext = kind === "gif" ? "gif" : blob.type.includes("mp4") ? "mp4" : "webm";
              const name = `${(info.mapName || "map").replace(/[\\/:*?"<>|]+/g, " ").trim()} timelapse.${ext}`;
              status.set(t("{size} \u2014 choose where to save it", { size: formatBytes(blob.size) }));
              const saved = await api.ui.saveFile(blob, name);
              if (!saved) {
                status.set(t("Not saved."));
                return false;
              }
              api.ui.toast({ kind: "ok", title: t("Timelapse saved"), detail: `${saved.fileName} \xB7 ${formatBytes(blob.size)}` });
              return true;
            } catch (err) {
              status.cancel(null);
              if (signal.aborted) {
                status.set(t("Stopped."));
                return false;
              }
              api.log("Export failed", err);
              status.set(t("The export failed: {error}", { error: err instanceof Error ? err.message : String(err) }), "error");
              return false;
            } finally {
              abort = null;
            }
          }
        }
      ]
    });
  }
  /* ── Preferences ── */
  preferencesPage() {
    const { api, t } = this;
    api.ui.preferencesPage({
      mount: (body) => {
        const w = api.ui.widgets;
        const auto = w.checkbox(t("Record every map while I edit it"), { value: this.settings.auto, onChange: (v) => this.save({ auto: v }) });
        const keep = w.number({ value: this.settings.keep, min: 1, max: 500, step: 1, onChange: (v) => this.save({ keep: Math.max(1, Math.round(v) || DEFAULTS.keep) }) });
        const usage = w.hint("");
        const showUsage = () => {
          const bytes = this.infos.reduce((n, i) => n + i.bytes, 0);
          usage.textContent = this.memoryOnly ? t("The browser's storage is not available, so recordings last only until the page is reloaded.") : t("{n, plural, one {# recording} other {# recordings}}, about {size}, kept in this browser.", { n: this.infos.length, size: formatBytes(bytes) });
        };
        showUsage();
        const clear = w.button(t("Delete all recordings\u2026"), {
          danger: true,
          onClick: async () => {
            if (!await api.ui.confirm(t("Delete every recording? This cannot be undone."), { danger: true, confirmLabel: t("Delete all") })) return;
            await this.removeAll();
            showUsage();
          }
        });
        body.append(
          w.group(
            t("Recording"),
            auto,
            w.hint(t("Off, nothing is recorded until you press Record in the Timelapse panel.")),
            w.form([{ label: t("Recordings to keep"), field: keep }]),
            w.hint(t("When a new recording starts, the oldest beyond this number are deleted."))
          ),
          w.group(t("Storage"), usage, w.row(clear))
        );
      },
      reset: () => {
        this.save({ auto: DEFAULTS.auto, keep: DEFAULTS.keep });
      }
    });
  }
  disposed = false;
  dispose() {
    this.disposed = true;
    this.player?.stop();
    this.panel?.close();
    this.status?.remove();
    this.library?.close();
    this.style.remove();
  }
};
var Player = class {
  root;
  /** The recording on show. */
  showing = null;
  frames = [];
  scene = null;
  stage = null;
  index = 0;
  timer = null;
  graphicsMissing = false;
  loadSerial = 0;
  picker;
  stageBox;
  slider;
  count;
  playButton;
  caption;
  foot;
  app;
  constructor(app) {
    this.app = app;
    const { api, t } = app;
    const w = api.ui.widgets;
    const s = app.settings;
    this.picker = w.select([], { onChange: (id) => void this.show(id) });
    this.stageBox = h("div", { className: "tl-stage" });
    this.slider = h("input", { type: "range", min: "0", max: "0", value: "0" });
    this.slider.addEventListener("input", () => {
      this.pause();
      this.goTo(Number(this.slider.value));
    });
    this.count = h("span", { className: "tl-count" });
    this.playButton = w.button("", { title: t("Play"), onClick: () => this.togglePlay() });
    this.playButton.append(icon("play"));
    const first = w.button("", { title: t("First frame"), onClick: () => {
      this.pause();
      this.goTo(0);
    } });
    first.append(icon("first"));
    const last = w.button("", { title: t("Last frame"), onClick: () => {
      this.pause();
      this.goTo(this.frames.length - 1);
    } });
    last.append(icon("last"));
    this.caption = h("div", { className: "tl-caption" });
    const fps = w.select([2, 5, 8, 12, 20, 30, 60].map((n) => ({ value: n, label: t("{n} fps", { n }) })), {
      value: s.fps,
      onChange: (v) => {
        app.save({ fps: Number(v) });
        if (this.timer) {
          this.pause();
          this.play();
        }
      }
    });
    const tick2 = (key, label) => w.checkbox(label, { value: s[key], onChange: (v) => {
      app.save({ [key]: v });
      this.redraw();
    } });
    this.foot = h("div", { className: "tl-foot" });
    this.root = h(
      "div",
      { className: "tl" },
      h("div", { className: "tl-bar" }, this.picker),
      this.stageBox,
      h("div", { className: "tl-controls" }, first, this.playButton, last, this.slider, this.count),
      this.caption,
      h("div", { className: "tl-opts" }, fps, tick2("units", t("Units")), tick2("sprites", t("Doodads and sprites")), tick2("locations", t("Locations")), tick2("highlight", t("Box around each change"))),
      this.foot
    );
  }
  /** Fill the recording picker, and show the front map's recording (or the newest) if nothing is on show yet. */
  refreshList() {
    const { t } = this.app;
    const liveIds = new Map([...this.app.live.values()].map((l) => [l.info.id, l]));
    this.picker.replaceChildren(...this.app.infos.map((info) => {
      const live = liveIds.get(info.id);
      const when = new Date(info.updated).toLocaleString(void 0, { dateStyle: "medium", timeStyle: "short" });
      const mark = live ? live.paused ? t(" (paused)") : t(" (recording)") : "";
      return h("option", { value: info.id }, `${info.mapName} \u2014 ${when}${mark}`);
    }));
    const stillThere = this.showing !== null && this.app.infos.some((i) => i.id === this.showing);
    if (!stillThere) {
      const target = this.app.front()?.info.id ?? this.app.infos[0]?.id ?? null;
      this.showing = null;
      if (target) void this.show(target);
      else this.empty();
    } else {
      this.picker.value = this.showing;
    }
    this.updateFooter();
  }
  /** The front map changed: show its recording, if it has one. */
  followDocument() {
    const live = this.app.front();
    if (live && live.info.id !== this.showing) void this.show(live.info.id);
    this.refreshList();
  }
  empty() {
    const { t } = this.app;
    this.stop();
    this.frames = [];
    this.stage = null;
    this.scene = null;
    this.stageBox.replaceChildren(h("div", { className: "tl-empty" }, this.app.settings.auto ? t("Nothing recorded yet. Change the map and every change becomes a frame here.") : t("Recording is off. Press Record to start recording the map in front.")));
    this.slider.max = "0";
    this.count.textContent = "";
    this.caption.replaceChildren();
  }
  async show(id) {
    const { t } = this.app;
    this.pause();
    this.showing = id;
    this.picker.value = id;
    const serial = ++this.loadSerial;
    const live = [...this.app.live.values()].find((l) => l.info.id === id);
    let frames;
    if (live) frames = live.frames;
    else {
      this.stageBox.replaceChildren(this.app.api.ui.widgets.spinner({ label: t("Loading the recording\u2026") }));
      try {
        frames = await this.app.library?.frames(id) ?? [];
      } catch {
        frames = [];
      }
      if (serial !== this.loadSerial) return;
    }
    this.frames = frames;
    if (frames.length === 0) {
      this.empty();
      return;
    }
    this.makeStage();
    this.graphicsMissing = false;
    for (const era of new Set(frames.map((f) => f.era))) {
      if (!await this.stage.prepare(era)) this.graphicsMissing = true;
      if (serial !== this.loadSerial) return;
    }
    this.scene = null;
    this.goTo(frames.length - 1);
    this.updateFooter();
  }
  makeStage() {
    const size = extent(this.frames);
    if (this.stage && this.stage.extent.width === size.width && this.stage.extent.height === size.height) return;
    this.stage = new Stage(this.app.api, fitPpt(size, 768), size);
    this.scene = null;
    this.stageBox.replaceChildren(this.stage.canvas);
  }
  goTo(index) {
    if (!this.stage || this.frames.length === 0) return;
    this.index = Math.max(0, Math.min(index, this.frames.length - 1));
    this.scene = seek(this.frames, this.index, this.scene);
    this.redraw();
  }
  redraw() {
    const { t } = this.app;
    if (!this.stage || !this.scene) return;
    const s = this.app.settings;
    const frame = this.frames[this.index];
    const ok = this.stage.draw(this.scene, { units: s.units, sprites: s.sprites, locations: s.locations, highlight: s.highlight ? frame.area : null });
    if (!ok && !this.graphicsMissing) this.graphicsMissing = true;
    if (this.graphicsMissing && this.stageBox.lastChild === this.stage.canvas) {
      this.stageBox.append(h("div", { className: "tl-empty", style: "position:relative" }, t("The tileset graphics this recording needs are not installed (Help \u25B8 Game Data\u2026).")));
    }
    this.slider.max = String(this.frames.length - 1);
    this.slider.value = String(this.index);
    this.count.textContent = `${this.index + 1} / ${this.frames.length}`;
    this.caption.replaceChildren(h("b", null, this.describe(frame)), h("span", { className: "grow" }), h("span", null, t("{time} in", { time: formatDuration(frame.t) })));
  }
  describe(frame) {
    const { t } = this.app;
    switch (frame.reason) {
      case "start":
        return t("The map when recording began");
      case "undo":
        return t("Undo {label}", { label: frame.label });
      case "redo":
        return t("Redo {label}", { label: frame.label });
      case "remote":
        return t("Someone else's edits");
      default:
        return frame.label || t("Map settings");
    }
  }
  /** A frame was added to a recording that is being taken. */
  framesAdded(live) {
    if (live.info.id !== this.showing) {
      if (this.frames.length === 0 && this.app.front() === live) void this.show(live.info.id);
      return;
    }
    if (this.frames !== live.frames) this.frames = live.frames;
    const size = extent(this.frames);
    if (!this.stage || size.width > this.stage.extent.width || size.height > this.stage.extent.height) this.makeStage();
    if (!this.timer && this.index >= this.frames.length - 2) this.goTo(this.frames.length - 1);
    else {
      this.slider.max = String(this.frames.length - 1);
      this.count.textContent = `${this.index + 1} / ${this.frames.length}`;
    }
  }
  togglePlay() {
    if (this.timer) this.pause();
    else this.play();
  }
  play() {
    if (this.frames.length < 2) return;
    if (this.index >= this.frames.length - 1) {
      this.goTo(0);
    }
    this.playButton.replaceChildren(icon("pause"));
    this.playButton.title = this.app.t("Pause");
    this.timer = setInterval(() => {
      if (this.index >= this.frames.length - 1) {
        this.pause();
        return;
      }
      this.goTo(this.index + 1);
    }, 1e3 / this.app.settings.fps);
  }
  pause() {
    if (this.timer) clearInterval(this.timer);
    this.timer = null;
    this.playButton.replaceChildren(icon("play"));
    this.playButton.title = this.app.t("Play");
  }
  stop() {
    this.pause();
    this.loadSerial++;
  }
  updateFooter() {
    const { api, t } = this.app;
    const w = api.ui.widgets;
    const live = this.app.front();
    const info = this.app.infos.find((i) => i.id === this.showing) ?? null;
    const record = !api.document.isOpen() ? null : !live ? w.button(t("Record"), { title: t("Start recording the map in front"), onClick: () => this.app.toggleRecording() }) : w.button(live.paused ? t("Resume recording") : t("Pause recording"), { onClick: () => this.app.toggleRecording() });
    if (record && live && !live.paused) record.prepend(h("span", { className: "tl-dot" }));
    const summary = info ? h("span", { className: "tl-count" }, t("{time} of editing \xB7 {size}", { time: formatDuration(info.elapsed), size: formatBytes(info.bytes) })) : null;
    this.foot.replaceChildren(...[
      w.button(t("Export\u2026"), { primary: true, disabled: this.frames.length < 2 || !info, onClick: () => {
        if (info) {
          this.pause();
          this.app.exportDialog(info, this.frames);
        }
      } }),
      w.button(t("Delete\u2026"), {
        disabled: !info,
        onClick: async () => {
          if (!info) return;
          if (!await api.ui.confirm(t("Delete the recording of \u201C{name}\u201D? This cannot be undone.", { name: info.mapName }), { danger: true, confirmLabel: t("Delete") })) return;
          this.showing = null;
          await this.app.remove(info.id);
        }
      }),
      h("span", { className: "grow" }),
      summary,
      record
    ].filter((n) => n !== null));
  }
};
function activate(api) {
  api.i18n.register({ ko: KO });
  const app = new Timelapse(api);
  api.commands.register({ id: "open", title: "Timelapse", run: () => app.togglePanel() });
  api.commands.register({ id: "record", title: "Record or Pause Timelapse", run: () => app.toggleRecording() });
  api.menu.add("View", { label: app.t("Timelapse\u2026"), icon: "plugin", command: "open" });
  api.events.on("commit", (e) => app.onCommit(e));
  api.events.on("document", (e) => app.onDocument(e));
  api.events.on("file", () => app.onFile());
  api.events.on("gameData", () => app.player?.redraw());
  api.events.on("language", () => app.relabel());
  app.preferencesPage();
  void app.init().then(() => {
    if (app.settings.open) app.openPanel();
  });
  return () => app.dispose();
}
export {
  activate as default
};
